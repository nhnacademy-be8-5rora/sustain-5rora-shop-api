eureka:
  client:
    enabled: false
file:
  upload-dir: ${user.home}/app-uploads # 각자 홈 디렉토리에 폴더 생성 후 저장됨

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB      # 파일 크기 제한
      max-request-size: 10MB